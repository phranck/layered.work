{
  "name": "@layered/content",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The content language: register, parser, renderer, validator",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "development": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json && node scripts/reference.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "@layered/tokens": "workspace:*",
    "@lezer/common": "^1.5.2",
    "@lezer/markdown": "^1.7.2"
  },
  "devDependencies": {
    "vitest": "^3.2.7"
  }
}
