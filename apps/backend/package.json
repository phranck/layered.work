{
  "name": "@layered/backend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The API on api.layered.work",
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "node --watch --env-file-if-exists=../../.env.local --experimental-strip-types src/index.ts",
    "typecheck": "tsc --noEmit",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "pnpm build && node --env-file-if-exists=../../.env.local dist/db/migrate.js",
    "db:seed": "pnpm build && node --env-file-if-exists=../../.env.local dist/db/seed.js"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2",
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.10"
  }
}
